# StellStringTasks
 Код представляет собой набор функций для выполнения базовых операций со строками в C++: удаление и вставка символов, замена подстрок, подсчет вхождений и классификация символов




 🛠️ПРИМЕЧАНИЕ: не работает русская раскладка, или точнее код по умолчанию работают только с ASCII символами (латиницей), а не с кириллицей (Unicode/UTF-8),
 поэтому лучше всего вводить внглийские слова. Для решения нужно подключать библиотеку #include locale , не разобрался пока как она работает, постараюсь доработать 
 этот момент в коде.
